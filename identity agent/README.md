# Identity Agent

## Structure

- `src/`: identity service code
- `db/`: PostgreSQL schema and seed SQL
- `docs/`: setup notes

## Run

From the `identity agent/` folder:

```powershell
python .\src\driver.py
```
